<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="pethubpackage.dao.PetDao">

    <select id="getAllPet">
        SELECT 
            *
        FROM
            pet_tbl
    </select>

    <select id="getPet">
        SELECT 
            *
        FROM
            pet_tbl
        WHERE
            pet_id = #{pet_id}
    </select>
    
    <insert id="addPet">
        INSERT INTO
            pet_tbl (pet_name, pet_specie_id, pet_breed, pet_gender_id, pet_age, pet_description, prev_guardian_id, new_guardian_id, is_available_to_adopt, adoption_date_time, admission_date_time)
        VALUES
            (#{pet_name}, #{pet_specie_id}, #{pet_breed}, #{pet_gender_id}, #{pet_age}, #{pet_description}, #{prev_guardian_id}, #{new_guardian_id}, #{is_available_to_adopt}, #{adoption_date_time}, #{admission_date_time})
    </insert>
</mapper>